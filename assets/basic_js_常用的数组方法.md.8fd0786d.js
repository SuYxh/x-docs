import{o as A,c as p,e}from"./app.8b525408.js";const a='{"title":"","description":"","frontmatter":{},"relativePath":"basic/js/常用的数组方法.md","lastUpdated":1658753104216}',g={},i=[e('<p><img src="https://qn.huat.xyz/mac/20220725203926.jpeg" alt="图片"></p><p>英文 | <a href="https://javascript.plainenglish.io/20-most-used-array-methods-in-javascript-c57276982377" target="_blank" rel="noopener noreferrer">https://javascript.plainenglish.io/20-most-used-array-methods-in-javascript-c57276982377</a></p><p>翻译 | 杨小爱</p><p>在 JavaScript 中，一个数组实例有 37 个内置方法，常用的方法大约有 20 个。在这篇文章中，我对这些方法进行了总结，并附上了一些图表，希望能帮助你更好地整理相关知识点。</p><p><strong>push, pop, shift, unshift</strong></p><p>这四种方法非常相似，我们一起来谈谈。</p><ul><li><p>push(element) ：将一个或多个元素添加到数组的末尾，并返回数组的新长度。</p></li><li><p>pop()：从数组中删除最后一个元素并返回该元素。</p></li><li><p>shift() ：从数组中移除第一个元素并返回移除的元素。</p></li><li><p>unshift() ：将一个或多个元素添加到数组的开头并返回数组的新长度。</p></li></ul><p>以下是一些操作：</p><p><img src="https://qn.huat.xyz/mac/20220725203931.png" alt="图片"></p><p>以下是一些操作：</p><p><img src="https://qn.huat.xyz/mac/20220725203937.png" alt="图片"></p><p>测试一下：</p><p><strong>push：</strong></p><div class="language-"><pre><code>const animals = [&#39;pigs&#39;, &#39;goats&#39;, &#39;sheep&#39;];\n</code></pre></div><p><strong>shift：</strong></p><div class="language-"><pre><code>const myFish = [&#39;angel&#39;, &#39;clown&#39;, &#39;mandarin&#39;, &#39;surgeon&#39;];\n</code></pre></div><p><strong>unshift:</strong></p><div class="language-"><pre><code>const array1 = [1, 2, 3];\n</code></pre></div><p><strong>slice</strong></p><ul><li><p>slice() 方法返回数组一部分的浅拷贝。</p></li><li><p>它返回一个新数组，并且不会修改原始数组。</p></li></ul><p><img src="https://qn.huat.xyz/mac/20220725203943.png" alt="图片"></p><p>语法：</p><div class="language-"><pre><code>array.slice(startIndex, endIndex)\n</code></pre></div><p>测试一下：</p><div class="language-"><pre><code>let arr = [&#39;a&#39;, &#39;b&#39;, &#39;c&#39;, &#39;d&#39;, &#39;e&#39;, &#39;f&#39;]\n</code></pre></div><p><strong>splice</strong></p><p>splice() 方法通过删除或替换现有元素和/或在适当位置添加新元素来更改数组的内容。</p><p>语法：</p><div class="language-"><pre><code>splice(start)\n</code></pre></div><ul><li><p>start：开始更改数组的索引。</p></li><li><p>deleteCount：一个整数，指示数组中要从开始删除的元素数。</p></li><li><p>item1, item2, ...：要添加到数组中的元素，从 start 开始。</p></li></ul><p><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="图片"></p><p><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="图片"></p><p>测试一下：</p><div class="language-"><pre><code>let myFish = [&#39;angel&#39;, &#39;clown&#39;, &#39;mandarin&#39;, &#39;sturgeon&#39;]\n</code></pre></div><div class="language-"><pre><code>let myFish = [&#39;angel&#39;, &#39;clown&#39;, &#39;mandarin&#39;, &#39;sturgeon&#39;]\n</code></pre></div><p><strong>concat</strong></p><ul><li><p>concat() 方法用于合并两个或多个数组。</p></li><li><p>此方法不会更改现有数组，而是返回一个新数组。</p></li></ul><p><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="图片"></p><p>测试一下：</p><div class="language-"><pre><code>const array1 = [&#39;a&#39;, &#39;b&#39;, &#39;c&#39;];\n</code></pre></div><p><strong>join</strong></p><p>join() 方法通过连接数组中的所有元素创建并返回一个新字符串，该数组由逗号或指定的分隔符字符串分隔。</p><p>语法：</p><div class="language-"><pre><code>join(separator)\n</code></pre></div><p><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="图片"></p><p>测试一下：</p><div class="language-"><pre><code>let arr = [&#39;a&#39;, &#39;b&#39;, &#39;c&#39;, &#39;d&#39;];\n</code></pre></div><p>你可以猜一猜他们的输出结果是多少？</p><p><strong>every</strong></p><p>every() 方法测试数组中的所有元素是否通过提供的函数实现的测试，它返回一个布尔值。</p><p><img src="https://qn.huat.xyz/mac/20220725203953.png" alt="图片"></p><p>测试一下：</p><div class="language-"><pre><code>function isEven(num){\n</code></pre></div><p><strong>some</strong></p><ul><li><p>some() 方法测试数组中的至少一个元素是否通过了提供的函数实现的测试。</p></li><li><p>如果在数组中找到所提供函数为其返回 true 的元素，则返回 true；</p></li><li><p>否则，它返回 false。</p></li></ul><p><img src="https://qn.huat.xyz/mac/20220725203958.png" alt="图片"></p><p>它不会修改数组。</p><p>测试一下：</p><div class="language-"><pre><code>function isEven(num){\n</code></pre></div><p><strong>map</strong></p><p>map() 方法创建一个新数组，其中填充了在调用数组中的每个元素上调用提供的函数的结果。</p><p>语法：</p><div class="language-"><pre><code>// Arrow function\n</code></pre></div><p>测试一下</p><div class="language-"><pre><code>const double = ele =&gt; ele * 2\n</code></pre></div><p><strong>reduce</strong></p><p>reduce() 方法按顺序对数组的每个元素执行用户提供的“reducer”回调函数，传入前一个元素的计算返回值。在数组的所有元素上运行 reducer 的最终结果是单个值。</p><div class="language-"><pre><code>reduce((previousValue, currentValue) =&gt; { /* ... */ } )\n</code></pre></div><p>我知道上面抽象的概念不好理解，我们来看一个例子。</p><div class="language-"><pre><code>var arr = [&#39;a&#39;, &#39;b&#39;, &#39;c&#39;, &#39;d&#39;, &#39;e&#39;];\n</code></pre></div><p>这段代码是如何执行的？</p><p>在这段代码中，reducer 是 add 。</p><p>首先，因为我们是第一次执行add，所以数组中的第一个元素&#39;a&#39;会被当作add的第一个参数，然后循环会从数组的第二个元素&#39;b&#39;开始。这一次，&#39;b&#39; 是 add 的第二个参数。</p><p><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="图片"></p><p>第一次计算后，我们得到结果&#39;ab&#39;。此结果将被缓存并在下一次添加计算中用作 param1。同时，数组中的第三个参数&#39;c&#39;将用作add的param2。</p><p><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="图片"></p><p>同样，reduce 会继续遍历数组中的元素，运行 &#39;abc&#39; 和 &#39;d&#39; 作为 add 的参数。</p><p><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="图片"></p><p>最后，遍历数组中最后一个元素后，返回计算结果。</p><p><img src="https://qn.huat.xyz/mac/20220725204003.png" alt="图片"></p><p>现在我们有了结果：&#39;abcde&#39;。</p><div class="language-"><pre><code>var arr = [&#39;a&#39;, &#39;b&#39;, &#39;c&#39;, &#39;d&#39;, &#39;e&#39;];\n</code></pre></div><p><strong>filter</strong></p><p>filter() 方法创建一个新数组，其中包含通过所提供函数实现的测试的所有元素。</p><p><img src="https://qn.huat.xyz/mac/20220725204007.png" alt="图片"></p><p>测试一下</p><div class="language-"><pre><code>function isEven(num){\n</code></pre></div><p><strong>fill</strong></p><p>fill() 方法将数组中的所有元素更改为静态值，从开始索引（默认 0）到结束索引（默认 array.length）。它返回修改后的数组。</p><p>语法：</p><div class="language-"><pre><code>fill(value)\n</code></pre></div><p><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="图片"></p><p><strong>find, findLast, findIndex, findLastIndex</strong></p><ul><li><p>find() ：返回数组中满足提供的测试函数的第一个元素的值，如果没有找到合适的元素，则返回 undefined。</p></li><li><p>findIndex() ：返回数组中满足提供的测试函数的第一个元素的索引，如果没有找到合适的元素，则返回 -1。</p></li><li><p>findLast() ：返回数组中满足提供的测试函数的最后一个元素的值，如果没有找到合适的元素，则返回 undefined。</p></li><li><p>findLastIndex() ：返回数组中满足提供的测试函数的最后一个元素的索引，如果没有找到合适的元素，则返回 -1。</p></li></ul><p><img src="data:image/gif;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVQImWNgYGBgAAAABQABh6FO1AAAAABJRU5ErkJggg==" alt="图片"></p><p>测试一下：</p><div class="language-"><pre><code>let arr = [2, &#39;a&#39;, 7, 0, &#39;a&#39;, 0]\n</code></pre></div><p><strong>总结</strong></p><p>以上就是我今天跟你分享的关于JavaScript常用数组的总结，如果你觉得有用的话，请记得点赞我，关注我，并将它分享给你身边做开发的朋友，也许能够帮助到他。</p><p>最后，感谢你的阅读，祝编程愉快！</p>',100)];g.render=function(e,a,g,r,s,n){return A(),p("div",null,i)};export default g;export{a as __pageData};
